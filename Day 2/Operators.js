// && (AND operator)
console.log(10 > 6 && 5 < 9) //both true than true

// || (OR Operator)
console.log(10 < 6 || 15 < 9 || 18 > 9) //one side is required to be true

// Uninary Operator
// post : first use than change
// pre first change than use